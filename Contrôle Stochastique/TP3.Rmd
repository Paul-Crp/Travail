---
title: "TP3 : Résolution de MDP à l'espace d'état infini"
author: "Paul Crespin"
output: pdf_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
```

# Simulations

## Question 1

On commence par faire un simulateur de MDP avec la politique 0, consistant à ne jamais rien faire.
C'est un modèle de survie simplifié, on va considérer que chaque oiseau a une probabilité de mourir et une probabilité de se reproduire tout seul.
On va appliquer la loi binomiale oiseau par oiseau.
On va conserver dans un historique le nombre d'oiseaux et le coût en fonction du temps.

```{r, warning=FALSE, message=FALSE}

# On fixe les variables de bases

nb_oiseaux <- 2
nb_annees <- 20
c <- 0
historique_nb_oiseaux <- c(nb_oiseaux)
historique_c <- c(c)
alpha0 <- 5
proba_d <- 0.45
proba_b <- 0.45

# On fait la boucle sur le nombre d'années avec 2 nouvelles lois uniformes pour
# chaque oiseaux, chaque jours ainsi une actualisation du nombre d'oiseaux.

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
  }
  historique_nb_oiseaux[i] <- nb_oiseaux
  historique_c[i] <- c
}

# On ajoute la récompense finale

if (nb_oiseaux == 0) {
  c <- c - alpha0
} else {
  c <- c + nb_oiseaux
}

historique_c[nb_annees + 2] <- c
```

## Question 2

On va tracer quelques courbes de la politique 0.

```{r message=FALSE, warning=FALSE}
par(mfrow <- c(1, 2))
plot(c(0:nb_annees), historique_nb_oiseaux, type = "b")
plot(c(0:(nb_annees + 1)), historique_c, type = "b")
```

```{r message=FALSE, warning=FALSE, include=FALSE}
nb_oiseaux <- 2
nb_annees <- 20
c <- 0
historique_nb_oiseaux2 <- c(nb_oiseaux)
historique_c2 <- c(c)
alpha0 <- 5
proba_d <- 0.45
proba_b <- 0.45

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
  }
  historique_nb_oiseaux2[i] <- nb_oiseaux
  historique_c2[i] <- c
}

# On ajoute la récompense finale
if (nb_oiseaux == 0) {
  c <- c - alpha0
} else {
  c <- c + nb_oiseaux
}

historique_c2[nb_annees + 2] <- c

nb_oiseaux <- 2
c <- 0
historique_nb_oiseaux3 <- c(nb_oiseaux)
historique_c3 <- c(c)

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
  }
  historique_nb_oiseaux3[i] <- nb_oiseaux
  historique_c3[i] <- c
}

# On ajoute la récompense finale
if (nb_oiseaux == 0) {
  c <- c - alpha0
} else {
  c <- c + nb_oiseaux
}

historique_c3[nb_annees + 2] <- c

nb_oiseaux <- 2
c <- 0
historique_nb_oiseaux4 <- c(nb_oiseaux)
historique_c4 <- c(c)

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
  }
  historique_nb_oiseaux4[i] <- nb_oiseaux
  historique_c4[i] <- c
}

# On ajoute la récompense finale
if (nb_oiseaux == 0) {
  c <- c - alpha0
} else {
  c <- c + nb_oiseaux
}

historique_c4[nb_annees + 2] <- c
```

```{r echo=FALSE, message=FALSE, warning=FALSE, fig.height = 25, fig.width = 20}
par(mfrow <- c(3, 2))
plot(c(0:nb_annees), historique_nb_oiseaux2, type = "b")
plot(c(0:(nb_annees + 1)), historique_c2, type = "b")
plot(c(0:nb_annees), historique_nb_oiseaux3, type = "b")
plot(c(0:(nb_annees + 1)), historique_c3, type = "b")
plot(c(0:nb_annees), historique_nb_oiseaux4, type = "b")
plot(c(0:(nb_annees + 1)), historique_c4, type = "b")
```

On voit que la politique 0 est assez aléatoire et la survie des oiseaux est difficile.
On voit aussi que le coût ne varie qu'à la dernière année, au moment de "faire les comptes".

Le point principal à retenir est que, bien que la probabilité de naissances et de décès soient la même, la conséquence du décès est plus importante que celle de la naissance
En effet, si tous les oiseaux sont morts, c'est terminé.

## Question 3

On finit par estimer le coût de la politique 0 par la méthode de Monte-Carlo.

On va donc faire tourner 100 000 fois notre simulateur de la question 1 et calculer la moyenne des coûts et d'oiseaux survivants.

```{r message=FALSE, warning=FALSE}

# Variables de bases
nb_annees <- 20
alpha0 <- 5
proba_d <- 0.45
proba_b <- 0.45
nb_boucles <- 100000
p <- matrix(nrow = 2, ncol = nb_boucles)

# On crée une matrice à 2 lignes et autant de colonnes que nécessaire,
# la première pour le coût, la seconde pour le nombre d'oiseaux au bout de 20 ans.

for (ii in 1:nb_boucles) {

  nb_oiseaux <- 2
  c <- 0

  # Ces variables ont besoin d'être remises à 0 à chaque nouvelle boucle et on retire les historiques.

  for (i in 2:(nb_annees + 1)) {
    if (nb_oiseaux != 0) {
      for (x in 1:nb_oiseaux) {
        d <- runif(1, min = 0, max = 1)
        b <- runif(1, min = 0, max = 1)
        if (d < proba_d) {
          nb_oiseaux <- nb_oiseaux - 1
        }
        if (b < proba_b) {
         nb_oiseaux <- nb_oiseaux + 1
        }
      }
    }
  }

  # On ajoute la récompense finale
  if (nb_oiseaux == 0) {
    c <- c - alpha0
  } else {
    c <- c + nb_oiseaux
  }

  p[1, ii] <- c
  p[2, ii] <- nb_oiseaux

  # On rajoute le ii-ème gain final et nombre d'oiseaux à la ii-ème place de la matrice.

}

```

```{r message=FALSE, warning=FALSE}
print("Estimation du gain:")
rowMeans(p)
```

```{r message=FALSE, warning=FALSE}
v <- c()
v[1] <- (mean((p[1, ])^2) - mean(p[1, ])^2) / (nb_boucles - 1)
v[2] <- (mean((p[2, ])^2) - mean(p[2, ])^2) / (nb_boucles - 1)
# On ne multiplie pas par "nb_boucles" car on le divise par cette même quantité dans le calcul de l'IC.
```

```{r message=FALSE, warning=FALSE}
print("IC")
```

```{r message=FALSE, warning=FALSE}
print(rowMeans(p) - 1.96 * sqrt(v))
print(rowMeans(p) + 1.96 * sqrt(v))
```

On peut aussi calculer le nombre de fois où l'espèce a disparu, ce que l'on veut éviter à tout prix (enfin, au coût de 5 en l'occurence).
Ainsi que le nombre moyen d'oiseaux les fois où l'espèce n'a pas disparu, pour comparer avec les autres politiques.

```{r message=FALSE, warning=FALSE}
a <- 0
r <- c()
for (i in 1:nb_boucles) {
  if (p[2, i] > 0) {
    a <- a + 1
    r <- c(r, p[2, i])
  }
}
a / nb_boucles
mean(r)
```

On voit que le taux de survie est à peine de 28,372%, ce qui est très faible.
Cela entraine le fort coût de 5 dans 71,628% des cas. On voit bien l'influence importante de l'extinction de l'espèce, qui est irréversible.
De plus, on n'a que 7 oiseaux en moyenne quand l'espèce survit, ce qui est très peu.

## Question 4

On reprends le code de la question 1, on change la probabilité de naissance et on rajoute le coût de la politique 1.
Le fait de devoir continuer à payer même si l'espèce est éteinte, qui est sous-entendu par le fait de choisir l'action 1 à chaque pas dans le temps, n'est pas très cohérent, je calculerai donc un coût secondaire où on arrête de payer si l'espèce est éteinte.

```{r, warning=FALSE, message=FALSE}
nb_oiseaux <- 2
nb_annees <- 20
c <- 0
c1 <- 0
historique_nb_oiseaux <- c(nb_oiseaux)
historique_c <- c(c)
historique_c_bis <- c(c1)
alpha0 <- 5
alpha1 <- 0.1 # On rajoute le coût de la politique 1.
proba_d <- 0.45
proba_b <- 0.5 # On change la probabilité de naissance.

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
  c1 <- c1 - alpha1
  }
  c <- c - alpha1
  historique_nb_oiseaux[i] <- nb_oiseaux
  historique_c[i] <- c
  historique_c_bis[i] <- c1
}

if (nb_oiseaux == 0) {
  c <- c - alpha0
  c1 <- c1 - alpha0
} else {
  c <- c + nb_oiseaux
  c1 <- c1 + nb_oiseaux
}

historique_c[nb_annees + 2] <- c
historique_c_bis[nb_annees + 2] <- c1
```

## Question 5

On va tracer quelques courbes de la politique 1.

```{r message=FALSE, warning=FALSE}
par(mfrow <- c(1, 2))
plot(c(0:nb_annees), historique_nb_oiseaux, type = "b")
plot(c(0:(nb_annees + 1)), historique_c, type = "b")
```

```{r message=FALSE, warning=FALSE, include=FALSE}
nb_oiseaux <- 2
nb_annees <- 20
c <- 0
c1 <- 0
historique_nb_oiseaux2 <- c(nb_oiseaux)
historique_c2 <- c(c)
historique_c_bis2 <- c(c1)
alpha0 <- 5
alpha1 <- 0.1
proba_d <- 0.45
proba_b <- 0.5

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
  c1 <- c1 - alpha1
  }
  c <- c - alpha1
  historique_nb_oiseaux2[i] <- nb_oiseaux
  historique_c2[i] <- c
  historique_c_bis2[i] <- c1
}

if (nb_oiseaux == 0) {
  c <- c - alpha0
  c1 <- c1 - alpha0
} else {
  c <- c + nb_oiseaux
  c1 <- c1 + nb_oiseaux
}

historique_c2[nb_annees + 2] <- c
historique_c_bis2[nb_annees + 2] <- c1

nb_oiseaux <- 2
c <- 0
c1 <- 0
historique_nb_oiseaux3 <- c(nb_oiseaux)
historique_c3 <- c(c)
historique_c_bis3 <- c(c1)

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
  c1 <- c1 - alpha1
  }
  c <- c - alpha1
  historique_nb_oiseaux3[i] <- nb_oiseaux
  historique_c3[i] <- c
  historique_c_bis3[i] <- c1
}

if (nb_oiseaux == 0) {
  c <- c - alpha0
  c1 <- c1 - alpha0
} else {
  c <- c + nb_oiseaux
  c1 <- c1 + nb_oiseaux
}

historique_c3[nb_annees + 2] <- c
historique_c_bis3[nb_annees + 2] <- c1

nb_oiseaux <- 2
c <- 0
c1 <- 0
historique_nb_oiseaux4 <- c(nb_oiseaux)
historique_c4 <- c(c)
historique_c_bis4 <- c(c1)

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
  c1 <- c1 - alpha1
  }
  c <- c - alpha1
  historique_nb_oiseaux4[i] <- nb_oiseaux
  historique_c4[i] <- c
  historique_c_bis4[i] <- c1
}

if (nb_oiseaux == 0) {
  c <- c - alpha0
  c1 <- c1 - alpha0
} else {
  c <- c + nb_oiseaux
  c1 <- c1 + nb_oiseaux
}

historique_c4[nb_annees + 2] <- c
historique_c_bis4[nb_annees + 2] <- c1
```

```{r echo=FALSE, message=FALSE, warning=FALSE, fig.height = 25, fig.width = 20}
par(mfrow <- c(3, 2))
plot(c(0:nb_annees), historique_nb_oiseaux2, type = "b")
plot(c(0:(nb_annees + 1)), historique_c2, type = "b")
if (historique_c_bis2[nb_annees + 2] != historique_c2[nb_annees + 2]) {
  points(c(0:(nb_annees + 1)), historique_c_bis2, type = "b")
}
plot(c(0:nb_annees), historique_nb_oiseaux3, type = "b")
plot(c(0:(nb_annees + 1)), historique_c3, type = "b")
if (historique_c_bis3[nb_annees + 2] != historique_c3[nb_annees + 2]) {
  points(c(0:(nb_annees + 1)), historique_c_bis3, type = "b")
}
plot(c(0:nb_annees), historique_nb_oiseaux4, type = "b")
plot(c(0:(nb_annees + 1)), historique_c4, type = "b")
if (historique_c_bis4[nb_annees + 2] != historique_c4[nb_annees + 2]) {
  points(c(0:(nb_annees + 1)), historique_c_bis4, type = "b")
}
```

On voit que la politique 1 (qui consiste a favoriser les naissances) à l'air, sur ces quelques exemples, d'arriver moins souvent à l'extinction de l'espèce en favorisant le nombre d'indivudus et d'améliorer le gain.
Là où l'espèce s'éteint quand même, on a une perte légerement moindre si on arrête la protection et les coûts qui vont avec. 

## Question 6

On fait pareil que pour la question 3, on estime le coût de la politique 1 par la méthode de Monte-Carlo.

```{r message=FALSE, warning=FALSE}
nb_annees <- 20
alpha0 <- 5
alpha1 <- 0.1
proba_d <- 0.45
proba_b <- 0.5
nb_boucles <- 100000
p <- matrix(nrow = 3, ncol = nb_boucles)

for (ii in 1:nb_boucles) {

  nb_oiseaux <- 2
  c <- 0
  c1 <- 0

  for (i in 2:(nb_annees + 1)) {
    if (nb_oiseaux != 0) {
      for (x in 1:nb_oiseaux) {
        d <- runif(1, min = 0, max = 1)
        b <- runif(1, min = 0, max = 1)
        if (d < proba_d) {
          nb_oiseaux <- nb_oiseaux - 1
        }
        if (b < proba_b) {
          nb_oiseaux <- nb_oiseaux + 1
        }
      }
      c1 <- c1 - alpha1
    }
  c <- c - alpha1
  }

  if (nb_oiseaux == 0) {
    c <- c - alpha0
    c1 <- c1 - alpha0
  } else {
    c <- c + nb_oiseaux
    c1 <- c1 + nb_oiseaux
  }

  p[1, ii] <- c
  p[2, ii] <- c1
  p[3, ii] <- nb_oiseaux
}

```

```{r message=FALSE, warning=FALSE}
print("Estimation du gain:")
rowMeans(p)
```

```{r message=FALSE, warning=FALSE}
v <- c()
v[1] <- (mean((p[1, ])^2) - mean(p[1, ])^2) / (nb_boucles - 1)
v[2] <- (mean((p[2, ])^2) - mean(p[2, ])^2) / (nb_boucles - 1)
v[3] <- (mean((p[3, ])^2) - mean(p[3, ])^2) / (nb_boucles - 1)
# On ne multiplie pas par "nb_boucles" car on le divise par cette même quantité dans le calcul de l'IC.
```

```{r message=FALSE, warning=FALSE}
print("IC")
```

```{r message=FALSE, warning=FALSE}
print(rowMeans(p) - 1.96 * sqrt(v))
print(rowMeans(p) + 1.96 * sqrt(v))
print(rowMeans(p)[2] - 1.96 * sqrt(v)[2] - (rowMeans(p) + 1.96 * sqrt(v))[1])
```

```{r message=FALSE, warning=FALSE}
a <- 0
r <- c()
for (i in 1:nb_boucles) {
  if (p[3, i] > 0) {
    a <- a + 1
    r <- c(r, p[3, i])
  }
}
a / nb_boucles
mean(r)
```

On a ici un taux de survie 15% plus élevé que pour la politique 0.
Parmi les fois où les oiseaux survivent, on a en moyenne 12 oiseaux de plus, soit 5 de plus qu'avant.
Le gain est positif et on a 2,5 fois plus d'oiseaux en moyenne.

## Question 7

On refait la même chose sauf que cette fois, on protège les individus existants et non les naissances. (Politique 2)

```{r, warning=FALSE, message=FALSE}
nb_oiseaux <- 2
nb_annees <- 20
c <- 0
c1 <- 0
historique_nb_oiseaux <- c(nb_oiseaux)
historique_c <- c(c)
historique_c_bis <- c(c1)
alpha0 <- 5
alpha2 <- 0.2 # On change l'alpha
proba_d <- 0.35 # On diminue la probabilité de décès
proba_b <- 0.45

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
    c1 <- c1 - alpha2
  }
  c <- c - alpha2
  historique_nb_oiseaux[i] <- nb_oiseaux
  historique_c[i] <- c
  historique_c_bis[i] <- c1
}

if (nb_oiseaux == 0) {
  c <- c - alpha0
  c1 <- c1 - alpha0
} else {
  c <- c + nb_oiseaux
  c1 <- c1 + nb_oiseaux
}

historique_c[nb_annees + 2] <- c
historique_c_bis[nb_annees + 2] <- c1
```

## Question 8

On trace les courbes de la politique 2.

```{r message=FALSE, warning=FALSE}
par(mfrow <- c(1, 2))
plot(c(0:nb_annees), historique_nb_oiseaux, type = "b")
plot(c(0:(nb_annees + 1)), historique_c, type = "b")
points(c(0:(nb_annees + 1)), historique_c_bis, type = "b")
```

```{r message=FALSE, warning=FALSE, include=FALSE}
nb_oiseaux <- 2
nb_annees <- 20
c <- 0
c1 <- 0
historique_nb_oiseaux2 <- c(nb_oiseaux)
historique_c2 <- c(c)
historique_c_bis2 <- c(c1)
alpha0 <- 5
alpha2 <- 0.2 # On change l'alpha
proba_d <- 0.35 # On change la probabilité de décès
proba_b <- 0.45

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
    c1 <- c1 - alpha2
  }
  c <- c - alpha2
  historique_nb_oiseaux2[i] <- nb_oiseaux
  historique_c2[i] <- c
  historique_c_bis2[i] <- c1
}

if (nb_oiseaux == 0) {
  c <- c - alpha0
  c1 <- c1 - alpha0
} else {
  c <- c + nb_oiseaux
  c1 <- c1 + nb_oiseaux
}

historique_c2[nb_annees + 2] <- c
historique_c_bis2[nb_annees + 2] <- c1

nb_oiseaux <- 2
c <- 0
c1 <- 0
historique_nb_oiseaux3 <- c(nb_oiseaux)
historique_c3 <- c(c)
historique_c_bis3 <- c(c1)

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
    c1 <- c1 - alpha2
  }
  c <- c - alpha2
  historique_nb_oiseaux3[i] <- nb_oiseaux
  historique_c3[i] <- c
  historique_c_bis3[i] <- c1
}

if (nb_oiseaux == 0) {
  c <- c - alpha0
  c1 <- c1 - alpha0
} else {
  c <- c + nb_oiseaux
  c1 <- c1 + nb_oiseaux
}

historique_c3[nb_annees + 2] <- c
historique_c_bis3[nb_annees + 2] <- c1

nb_oiseaux <- 2
c <- 0
c1 <- 0
historique_nb_oiseaux4 <- c(nb_oiseaux)
historique_c4 <- c(c)
historique_c_bis4 <- c(c1)

for (i in 2:(nb_annees + 1)) {
  if (nb_oiseaux != 0) {
    for (x in 1:nb_oiseaux) {
      d <- runif(1, min = 0, max = 1)
      b <- runif(1, min = 0, max = 1)
      if (d < proba_d) {
        nb_oiseaux <- nb_oiseaux - 1
      }
      if (b < proba_b) {
        nb_oiseaux <- nb_oiseaux + 1
      }
    }
    c1 <- c1 - alpha2
  }
  c <- c - alpha2
  historique_nb_oiseaux4[i] <- nb_oiseaux
  historique_c4[i] <- c
  historique_c_bis4[i] <- c1
}

if (nb_oiseaux == 0) {
  c <- c - alpha0
  c1 <- c1 - alpha0
} else {
  c <- c + nb_oiseaux
  c1 <- c1 + nb_oiseaux
}

historique_c4[nb_annees + 2] <- c
historique_c_bis4[nb_annees + 2] <- c1
```

```{r echo=FALSE, message=FALSE, warning=FALSE, fig.height = 25, fig.width = 20}
par(mfrow <- c(3, 2))
plot(c(0:nb_annees), historique_nb_oiseaux2, type = "b")
plot(c(0:(nb_annees + 1)), historique_c2, type = "b")
if (historique_c_bis2[nb_annees + 2] != historique_c2[nb_annees + 2]) {
  points(c(0:(nb_annees + 1)), historique_c_bis2, type = "b")
}
plot(c(0:nb_annees), historique_nb_oiseaux3, type = "b")
plot(c(0:(nb_annees + 1)), historique_c3, type = "b")
if (historique_c_bis3[nb_annees + 2] != historique_c3[nb_annees + 2]) {
  points(c(0:(nb_annees + 1)), historique_c_bis3, type = "b")
}
plot(c(0:nb_annees), historique_nb_oiseaux4, type = "b")
plot(c(0:(nb_annees + 1)), historique_c4, type = "b")
if (historique_c_bis4[nb_annees + 2] != historique_c4[nb_annees + 2]) {
  points(c(0:(nb_annees + 1)), historique_c_bis4, type = "b")
}
```

On voit que la politique 2 (qui consiste à protéger les individus existants) à l'air, sur ces quelques exemples, de non seulement arriver moins souvent à l'extinction, mais aussi d'avoir un fort nombre d'individus à la fin.

## Question 9

```{r message=FALSE, warning=FALSE}

nb_annees <- 20
alpha0 <- 5
alpha2 <- 0.2
proba_d <- 0.35
proba_b <- 0.45
nb_boucles <- 100000
p <- matrix(nrow = 3, ncol = nb_boucles)

for (ii in 1:nb_boucles) {

  nb_oiseaux <- 2
  c <- 0
  c1 <- 0

  for (i in 2:(nb_annees + 1)) {
    if (nb_oiseaux != 0) {
      for (x in 1:nb_oiseaux) {
        d <- runif(1, min = 0, max = 1)
        b <- runif(1, min = 0, max = 1)
        if (d < proba_d) {
          nb_oiseaux <- nb_oiseaux - 1
        }
        if (b < proba_b) {
          nb_oiseaux <- nb_oiseaux + 1
        }
      }
      c1 <- c1 - alpha2
    }
  c <- c - alpha2
  }

  if (nb_oiseaux == 0) {
    c <- c - alpha0
    c1 <- c1 - alpha0
  } else {
    c <- c + nb_oiseaux
    c1 <- c1 + nb_oiseaux
  }

  p[1, ii] <- c
  p[2, ii] <- c1
  p[3, ii] <- nb_oiseaux
}
```

```{r message=FALSE, warning=FALSE}
print("Estimation du gain:")
rowMeans(p)
```

```{r message=FALSE, warning=FALSE}
v <- c()
v[1] <- (mean((p[1, ])^2) - mean(p[1, ])^2) / (nb_boucles - 1)
v[2] <- (mean((p[2, ])^2) - mean(p[2, ])^2) / (nb_boucles - 1)
v[3] <- (mean((p[3, ])^2) - mean(p[3, ])^2) / (nb_boucles - 1)
# On ne multiplie pas par "nb_boucles" car on le divise par cette même quantité dans le calcul de l'IC.
```

```{r message=FALSE, warning=FALSE}
print("IC")
```

```{r message=FALSE, warning=FALSE}
print(rowMeans(p) - 1.96 * sqrt(v))
print(rowMeans(p) + 1.96 * sqrt(v))
print(rowMeans(p)[2] - 1.96 * sqrt(v)[2] - (rowMeans(p) + 1.96 * sqrt(v))[1])
```

```{r message=FALSE, warning=FALSE}
a <- 0
r <- c()
for (i in 1:nb_boucles) {
  if (p[3, i] > 0) {
    a <- a + 1
    r <- c(r, p[3, i])
  }
}
a / nb_boucles
mean(r)
```

On voit que le taux de survie crève le plafond (environ 60%) et est de 22 individus quand l'espèce survit.
Le gain est positif et on a 6,5 fois plus d'oiseaux en moyenne.

## Question 10

On cherche le nombre d'oiseaux maximal qu'on peut avoir au bout de 20 ans, en partant de 2.

C'est comme si on avait une probabilité de 0 de mourir et de 1 de naître.

Donc chaque année, chaque oiseau se "débouble". On a donc 2^21 = 2.097.152 oiseaux au bout de 20 ans.